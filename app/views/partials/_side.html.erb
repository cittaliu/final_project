<!-- aside -->
<aside id="aside" class="app-aside hidden-xs bg-dark">
  <div class="aside-wrap">
    <div class="navi-wrap">

      <!-- nav -->
      <nav ui-nav class="navi clearfix">
        <ul class="nav">
          <li class="hidden-folded padder m-t m-b-sm text-muted text-xs">
            <span>Navigation</span>
          </li>
          <li>
              <%=link_to dashboard_path do%>
              <i class="glyphicon glyphicon-stats icon text-primary-dker" ></i>
              <span class="font-bold">Dashboard</span>
              <%end%>
          </li>
          <li class="line dk"></li>

          <li class="hidden-folded padder m-t m-b-sm text-muted text-xs">
            <span>Opportunities</span>
          </li>

          <!-- refer to this when you need div as linke_to -->
          <li>
              <%=link_to opportunities_path(id: current_user.id) do%>
              <i class="glyphicon glyphicon-signal" ></i>
              <span class="font-bold">Opportunities</span>
              <%end%>
          </li>

          <li class="line dk"></li>

          <li class="hidden-folded padder m-t m-b-sm text-muted text-xs">
            <span>Contacts</span>
          </li>

          <li>
              <%=link_to contacts_path(id: current_user.id) do%>
              <i class="glyphicon glyphicon-signal" ></i>
              <span class="font-bold">Contacts</span>
              <%end%>
          </li>

        </ul>
      </nav>
      <!-- nav -->

    </div>
  </div>
</aside>
<!-- / aside -->
